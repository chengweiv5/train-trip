# 邯郸路线回归样本

- `material / initial / repaired`：从 2026-09-23 保存的邯郸搜索与模型输出缩减，只保留能复现“引用只含第一站、路线校验后为空”的两处景点和同一攻略正文。`repaired` 是覆盖这两站的连续原文，用于模拟修复响应。
- `phone-material / phone-initial / phone-repaired`：同日 Mate 60 Pro 实际整理的脱敏记录，删除图片及无关景点/美食，保留触发顺序错误、拼接引文和跨来源两日路线的原始段落。修复响应也是手机实际返回内容；一日路线可接受，第二天引用了当天不存在的景点，必须拒绝。

文档 URL 是来源归属，不在测试中联网访问。旅游事实、价格和开放信息并非独立验证的最新信息。样本不含密钥、请求头或个人设备标识。
