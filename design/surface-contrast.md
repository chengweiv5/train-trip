# 背景与卡片层次对比

日期：2026-09-21。状态：B4 冰蓝湖蓝与07顶部方案均已确认，现已实现于 Android v0.6.0；08补充已确认的出发时段多选。

## 查询目的地的想去分组

10画板 `F8QcC`、普通列表 `DJi6f`、空清单 `KQDys` 已实现并保存。“想去”固定在左侧省份最前，右侧按最近收藏显示城市及真实省份；与省份中的勾选同步，全选仅操作可查询的收藏。紧凑布局下，想去为省份下拉首项。已选省份仍按实际行政归属计算。

131个可编辑节点、56个文字节点，无裁切/同级重叠；设计及运行截图已核对，原生保存与目标节点回读一致。详见 [验收](../docs/verification/2026-09-21-wish-destinations.md)。

## 最长车程

09画板 `SapWP` 展示3小时预设 `TIQkt` 与自定义5小时30分 `QebZA`。常用时长改为3小时、5小时，保留不限；自定义使用小时/分钟双滚轮，点击完成保存，取消保留。窄屏或大字体下四选项改为两列。1分钟到100小时范围保持不变，0分钟不能完成，100小时时分钟固定00。

65个可编辑节点、30个文字节点，无裁切或同级重叠。原生保存后Edited标记消失、文件哈希变化，目标文档节点回读一致；后台独立副本打开返回占位画布，未完成独立副本回读，详见 [验收](../docs/verification/2026-09-21-duration-picker.md)。

## 出发时段多选

08画板 `ghTAc`、首页示例 `JnJJH` 展示“早上＋晚上”同时选中；沿用既有B4配色、顶部和五按钮单行。四个快捷段可独立勾选/取消，查询取并集；全天重置，取消最后一项或四项全选归一为全天。自定义仍为一个连续区间，完成后替换，取消保留原选择。

新画板含89个可编辑节点、35个文字节点，无裁切或同级重叠。原生保存后独立磁盘副本回读一致，07画板与修改前一致。内部设计截图和普通/大字体运行截图完成视觉与OCR核对；Pencil整屏缩略图的低置信识别结合节点原文复核。详见 [时段多选验收](../docs/verification/2026-09-21-departure-time-multi-select.md)。

## 最新设计反馈

用户确认 B4 配色，反馈顶部元素缺少风格、与卡片不协调。新增 07 画板 `NFNQM`，并排展示原首页、新首页、新想去清单，直接在 Pencil 确认。

- 顶部采用 `#DDF2FF` 到白色的冰蓝渐变，页面主体仍为白底浅蓝卡片。
- 首页用 32dp 湖蓝圆角火车标识与 18sp 应用名建立品牌识别；22sp 主标题“先看有票，再选目的地”，13sp 说明形成层级。
- 设置和添加城市统一为浅色圆角入口、湖蓝图标/文字、细描边；实际点击范围保留 48dp。
- 想去沿用相同顶部样式，星标和标题成组，收藏概况在标题下方。
- 设计稿首页首卡 y=164、想去首卡 y=136（含40dp系统栏）；实现使用系统实际安全区与自适应标题高度，正常字号分别为安全区后124dp／96dp。
- 配色对比的 B4 标题已标记“已选”；历史方案保留。顶部已于2026-09-21确认，并接入App。

实现与验证见 [v0.6.0 验收](../docs/verification/2026-09-21-v0.6.0-ui.md)。包含10画板的当前源文件 SHA256：`c24165ca819deabc536893275d3f3ecf5fd7e429fc0c57ecc8afb206580cbc10`。

以下为配色探索记录。

用户选择白色页面与浅色卡片的 B 布局，要求从整套色系搭配出发，不受现有蓝色、绿色限制；后续直接在 Pencil 确认，不再导出预览文件供确认。

第一组 B1 暖沙陶橙、B2 雾紫鸢尾、B3 燕麦墨青未获认可。之后比较 B4–B6，最终确认 B4；原稿保留作为历史记录：

| 方案 | 浅色卡片 | 主操作 | 气质 |
| --- | --- | --- | --- |
| B4 冰蓝湖蓝 | `#EAF5FC` | `#0073AA` | 清爽、通透，轻快出行工具 |
| B5 奶杏橙红 | `#FFF1E3` | `#BE401D` | 明亮、活泼，假日温度 |
| B6 雾白莓红 | `#F6F0F5` | `#C22A50` | 干净、利落，少量亮色突出操作 |

三套均同步搭配正文、辅助文字、内部控件、选中项和资料状态。白底保留，状态仍带文字/图标，不单靠颜色传意。主操作白字、浅色操作背景上的文字、卡片辅助文字均检查对比度。

Pencil 画板 `WxU7u`（05：首页）、`GJ2ZQ`（06：想去），共 542 个节点，无裁切/同级重叠；已做画布视觉检查。用户提出无需导出后，本组未导出 JPEG/PDF。前两轮产物仅作历史参考。

以下为第一轮对比的历史记录，推荐结论已被用户的 B 选择取代。

## 问题

当前页面背景为 `#F5F7FA`，普通卡片为白色且没有描边，两个明度接近的大面积区域不容易区分。主操作继续使用已确认的蓝色 `#1769E8`。

## 对比方案

| 项目 | 现状 | A：蓝灰底与白卡，推荐 | B：白底与浅色分组 |
| --- | --- | --- | --- |
| 页面背景 | `#F5F7FA` | `#EBF0F7` | `#FFFFFF` |
| 卡片背景 | `#FFFFFF` | `#FFFFFF` | `#EDF2F8` |
| 普通卡边界 | 无 | 1dp `#CFD9E7` | 1dp `#DCE5EF` |
| 选中项 | 浅蓝底与蓝字 | 保留浅蓝底与蓝字，补蓝色细描边 | 同 A |
| 观感 | 层次偏弱 | 白卡内容清楚，延续蓝白出行工具风格 | 更平整，浅色区域承担分组 |

推荐 A。通过页面底色与普通卡片边界建立层次，不使用厚重阴影；标题、正文、字号、间距、圆角和主按钮保持现有规格。普通卡片使用灰蓝边界，选择状态使用主蓝，避免混淆。

B 的浅蓝按钮增加 `#BCD3F6` 细边，避免与浅色卡片融为一体。两种方案均以同样内容和布局对比：查票首页、想去清单，各含现状/A/B 三张 390×920 屏幕。

选定后统一应用到查询、结果、车次详情、目的地介绍、想去和设置。实现时独立区分页面背景、卡片、内部控件表面与选中态，避免直接替换所有 `PageBackground` 引用而影响未选中控件。

## 设计文件

- 可编辑源：[train-trip-surface-contrast.pen](train-trip-surface-contrast.pen)
- 首页对比：[VpVyP.jpeg](reference/surface-contrast/VpVyP.jpeg)
- 想去对比：[dk9JK.jpeg](reference/surface-contrast/dk9JK.jpeg)
- 两页 PDF：[export.pdf](reference/surface-contrast/export.pdf)
- 画板：`VpVyP`（首页），`dk9JK`（想去）。源文件保留已有设计供参考。

## 验证与回滚

- 两个对比画板共 512 个节点；结构/坐标检查无裁切、无同级重叠。初稿和最终 JPEG 已视觉复核，导出尺寸 1250×1117。
- 宿主 Vision OCR 完成，首页 114 块、想去 101 块；图标存在 OCR 误识，文字与可编辑节点内容结合视觉复核。
- 原生保存后打开独立磁盘副本回读，两个画板结构、文字及变量与保存前完全一致。
- 原导航设计 SHA256 仍为 `b8c0ae973973be8f444a00824c36a55e6fd8df9ad6b68c55793e6b9678d01354`；新稿 SHA256 为 `f45c6b54608f50c1f4b6e387aeb8c36410f802fea6126c354939f3cff6f31f27`。
- 私有验证证据：`.verification-private/v0.5.0/surface-contrast/`。本次新增独立设计和导出；回滚只需移除本次新增产物，原稿及 App 不受影响。

## 后续快捷选择

“查询目的地”里快速选择想去城市已按用户提出的置顶分组方式实现，见本页10画板说明；早期背景与卡片对比稿保留为历史。


## v0.6.1 · 11 画板

沿用 B4 冰蓝湖蓝，增加想去省份标题与数量、左对齐子页标题、36dp 圆角返回容器（48dp 点击范围）、目的地横滑相册和上次检查记录。相册置于内容分页的可滚动区域；窄屏城市概览一并滚动，避免压缩正文。页面示例中的 1/3 表示多图交互状态，不代表内置天津资料已经新增三张图片；实际只展示成功保存的有来源图片。更新记录文案区分本次检查与历史成功，失败保留历史记录。

编辑源 `train-trip-surface-contrast.pen`，画板 `Ukt7E`；四页 `Q1xtJ`、`h1SvU`、`A1SWg`、`v4Icqr`。新增页面保持独立，旧已确认方案保留作参照。

当前应用名称更新为“有票再出发”，主设计文件中的可见品牌文字已同步。

## 想去双栏 · 12 画板（已实现）

用户补充：希望复用“查询目的地”选择器的左省份、右城市布局。原 v0.6.1 的省份标题纵向列表已替换为本节确认的双栏布局。

- `D27TnA`：12 · 想去双栏 / 评审说明。
- `fLUsq`：12A · 想去双栏 / 河北选中；承德、保定、秦皇岛按收藏时间从新到旧排列。
- `T33a9`：12B · 想去双栏 / 山东选中；青岛、济南、泰安展示切换后的城市列与内置介绍状态。

延续 B4 冰蓝湖蓝、现有顶部和底部“查票 / 想去”导航。左栏复用目的地选择器的 94dp 宽度、52dp 省份项、浅蓝底和选中高亮；只显示存在收藏的省份，并标注城市数量，省份顺序沿用当前行政目录。右侧只列出所选省份中的收藏，按 `addedAt` 降序，显示收藏日期以明确顺序。

城市卡片收紧为城市名、收藏日期、星标、介绍状态与两个入口；省份已在列标题显示，不在卡片内重复。保留“了解目的地”和“查车票”，点击卡片仍可进入介绍，收藏与查票点击区至少 48dp。示例城市、数量和日期仅用于设计展示，不读取真机收藏。

切换省份保留各自滚动位置；返回页面恢复选中省份。取消收藏仍可撤销；某省最后一座城市移除后切换到相邻可用省份，清单全空时沿用现有空状态。窄屏或大字体时延续目的地选择器的省份下拉适配，避免城市内容被侧栏挤压；Pencil 展示普通字号 390 × 844 双栏效果；实现另验证 320dp / 1.3 倍字体的下拉布局。

两张屏幕各 98 个可编辑节点、说明区 5 个节点，结构检查无裁切、无同级重叠；效果图和 OCR 复核通过（81 个文字区块，图标 OCR 误识按节点原文复核）。已通过原生保存并回读节点，文件 SHA256：`c5c15a5ab4d409bbea1f6b1649025ccbe1d9822e0166d1718693fd8e10c4ddf5`。备份与验证记录位于 `.verification-private/wishlist-sidebar/`。用户已确认并实现；左栏按目录拼音顺序展示，省内最近收藏优先。返回页面和 Activity 状态恢复保留选中省份及每省滚动位置；窄屏 / 大字体使用省份下拉。回滚使用本次提交的反向提交，历史设计和收藏存储保留。
