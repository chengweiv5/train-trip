# v0.6.1 Implementation Plan

> **For agentic workers:** Use superpowers:executing-plans to implement this plan task-by-task where available; otherwise execute inline with the same verification gates.

**Goal:** Complete the four confirmed Feishu feedback items.

**Architecture:** Keep presentation grouping separate from stored wishes. Extend the guide with a nullable photo list and an old-single-photo fallback; commit images incrementally. Store public update history independently of transient requests.

**Tech Stack:** Kotlin, Compose, Gson, AtomicFile, JUnit, Android instrumentation, Pencil.

## Global Constraints

- Version 0.6.1 / versionCode 11; preserve B4 palette and 48dp targets.
- Keep old saved wishes and single-photo guides readable; simplified Chinese sources only.
- No real paid service calls, no phone installation or public release in this task.
- Backups under .verification-private/v0.6.1/before; no force push or editing private Codex configuration.

### Task 1: Province grouping and shared headers

Files: core/.../Wishlist.kt, ui/WishlistScreen.kt, ui/AppTopBar.kt, ui/BrandHeader.kt, ui/TrainTripApp.kt.
- [ ] Add `wishlistGroups(items:List<WishCity>,catalog:StationCatalog):List<WishProvince>` using current catalog province, fallback saved province, stable catalog order, descending addedAt within each group. Add a regression for stale province and unknown city.
- [ ] Render province label/count and existing cards; use HeaderTop gradient and shared rounded HeaderIconButton on child pages; title uses weight, left alignment and wrap, actions reserve only their actual width.
- [ ] Verify normal and 320dp/1.3 font headers and existing wishlist flows.

### Task 2: Update history

Files: UpdateViewModel.kt, AndroidUpdateHistoryStore.kt, ui/AboutScreen.kt, ui/TrainTripApp.kt, V061Test.kt.
- [ ] Define `UpdateHistory(release,checkedAt,completedAt,error)` and `UpdateHistoryStore.read/write`; Android store uses AtomicFile and validates timestamps/version/URL, corrupt history falls back to empty.
- [ ] ViewModel loads history, cancels in-flight checks on leave but retains completed state; save success and failure. Cancel never changes completedAt. UI distinguishes historical results and allows manual recheck.
- [ ] Test recreate store/ViewModel, failure after success, cancellation and late completion; no live GitHub network.

### Task 3: Multiple photos

Files: core DestinationGuide.kt, TavilyGuideSource.kt, SearchGuideDecoder.kt, DeepSeekGuideGenerator.kt, SimplifiedGuidePolicy.kt; AndroidGuideStore.kt, DestinationViewModel.kt; DestinationGallery.kt, DestinationGuideScreen.kt, ResultsScreen.kt.
- [ ] Add nullable `photos` at end of constructor; `gallery` falls back to photo for legacy Gson; `withPhotos` keeps first photo compatible. Validate max 5, dedup and simplified data.
- [ ] Parse result and root images separately; root image must contain city + selected place; no fabricated page attribution. Strip image metadata from model prompt. Select up to 5 distinct validated images.
- [ ] Store text first and save each successful image with unique filename; cancel keeps committed text and images. Upgrade ownership, cleanup and shared-file checks to all images.
- [ ] Add gallery with count/caption/source, full-screen fit viewer, no-gallery empty behavior; results use first image. Keep gallery in scroll content to avoid crowding narrow screens.
- [ ] Test root-image attribution, foreign/traditional/decorative filtering, legacy cache, partial failure/cancel and deletion of all owned images.

### Task 4: Design and delivery verification

Files: design/train-trip-surface-contrast.pen, design/surface-contrast.md, app/build.gradle.kts, README.md, docs/verification/2026-09-21-v0.6.1.md.
- [ ] Add editable v0.6.1 wish/gallery/about/settings screens using existing Pencil nodes; structure and visual checks; save source.
- [ ] Run `./gradlew :core:test :app:assembleDebug :app:assembleDebugAndroidTest :app:assembleRelease :app:lintRelease`; expect all pass, no new lint errors.
- [ ] Run selected offline Android regressions on isolated emulator plus narrow font check, verify signed release and no bundled keys. Record exact results and limits, local commit and completion notification.
