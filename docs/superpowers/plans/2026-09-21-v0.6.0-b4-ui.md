# v0.6.0 B4 UI Implementation Plan

> 执行方式：在当前隔离工作树按步骤直接实现；已完成独立规格审阅。执行类子技能本机未安装，按本计划逐步执行与验证，不另建工作树。

**Goal:** 实现已确认的 B4 冰蓝湖蓝及07顶部样式，版本0.6.0/10。

**Architecture:** Theme 提供页面/卡片/控件/状态语义色，BrandHeader 封装两个根页面顶部，页面回调和ViewModel保持现有接口。根容器绘制与顶部连续的状态栏底色。复用现有界面回归，并补充会受布局改变影响的顶部/表面对比验证。

**Tech Stack:** Kotlin、Jetpack Compose Material3、Android API36、JDK21、现有离线仪器测试。

## Global Constraints

- 规格：`docs/superpowers/specs/2026-09-21-v0.6.0-b4-ui-design.md`，用户已批准设计和开发。
- `codex/v0.6.0-b4-ui` 开发；origin/main已刷新，落后0，当前无待合并PR。
- 不改网络、存储、密钥；不加入快速选择想去；不自动发布或安装真机。
- 无新依赖；320dp/1.3字号，至少48dp点击目标；只在Pencil确认设计。

## Task 1: 全局表面与版本

Files: `app/src/main/kotlin/cn/traintrip/app/ui/Theme.kt`、`Components.kt`、`DetailScreen.kt`、`DestinationGuideContent.kt`、`WishlistScreen.kt`、`SettingsScreen.kt`、`app/build.gradle.kts`。

- [x] 增加 CardBackground=#EAF5FC、CardBorder=#D5EAF6、ControlBackground=#FFFFFF、ContentReady=#446874、ActionBorder=#93CFE9。页面白色，Primary=#0073AA，PrimaryTint=#DDF2FF，Ink=#1E3443，Muted/Subtle=#516774，Line=#D5E5EE。
- [x] 普通ContentCard与景点/列车/添加城市卡改为CardBackground及1dp CardBorder；选中列车1.5dp Primary。Choice改为白底、选中浅蓝底及1dp Primary。
- [x] Material surface/container/outline引用对应语义，保持固定操作区域白色；介绍和设置就绪文字用ContentReady，余票成功仍保留原成功色与文字。
- [x] versionName改为0.6.0，versionCode改为10。

## Task 2: 统一根页面顶部

Files: create `app/src/main/kotlin/cn/traintrip/app/ui/BrandHeader.kt`; modify `AppTopBar.kt`、`FiltersScreen.kt`、`WishlistScreen.kt`、`TrainTripApp.kt`、`MainActivity.kt`。

Interfaces: `HomeBrandHeader(onSettings:()->Unit)`、`WishlistBrandHeader(summary:String,onAdd:()->Unit)`；共享内部顶部只接收文字和回调。

- [x] 顶部Column绘制HeaderTop到白色的verticalGradient；56dp导航行、32dp图标标识、18/22sp标题，图标及操作为湖蓝。
- [x] 设置外层IconButton48dp内36dp圆角底；添加城市48dp点击区内36dp视觉按钮。支持自然换行与动态高度。
- [x] 首页普通模式接HomeBrandHeader，单城查询保留AppTopBar；滚动内容从卡片开始，保留状态与原回调。
- [x] 想去概况移入WishlistBrandHeader，读取中/错误/空/有内容用明确文案；卡片按钮用浅蓝描边操作。保留收藏加载及LazyList状态。
- [x] 根容器只为查票/想去的状态栏绘制HeaderTop，安全区及底栏保持现有范围；系统栏固定浅色模式下深色图标。

## Task 3: 验证与交付

Files: `app/src/androidTest/kotlin/cn/traintrip/app/B4UiTest.kt`、`README.md`、`design/surface-contrast.md`、`docs/verification/2026-09-21-v0.6.0-ui.md`。

- [x] 复用BlueWhiteUiTest/WishlistFlowTest/ReleaseNotesLinkTest/DetailLayoutTest/SettingsMenuTest；增加普通与320dp/1.3字号下顶部无文本溢出、设置与添加回调、选中可见且表面分层的回归。
- [x] 宿主JDK21运行 `./gradlew :core:test :app:assembleDebug :app:assembleDebugAndroidTest :app:assembleRelease :app:lintRelease`；不为纯色常量编写镜像单测。
- [x] 启动专用train_trip_v060_test模拟器（旧临时AVD目录已失效），运行指定离线仪器测试，拉取截图检查首页/想去/结果/详情/介绍/设置/关于与大字体。关闭本任务模拟器，不操作真机。
- [x] 生成并验证生产签名Release候选包，版本/签名/不可调试及密钥扫描；不上传。
- [x] 更新README、设计确认状态、验收记录及TASK_STATE；提交当前批准的设计和实现，发送punk-12完成通知并回读；不推送或发布。

## 自检

规格覆盖：表面→任务1；顶部和安全区→任务2；版本、异常状态、响应式、交付→任务1/3。原功能通过现有离线回归保护。无独立后端、存储或协议改造。
