# 目的地更新入口左对齐

用户已确认：让「更新目的地介绍」与上方整理日期、下方「资料来源与图片署名」共用左边线，保留绿色文字及至少 48dp 的点击高度。

当前 `GuideRuntimeStatus` 的 TextButton 自带水平内边距，使文字比相邻内容向右缩进。只为带 `refresh-guide` 标签的按钮设置零水平、12dp 垂直内容内边距，并显式保留 48dp 最小高度。同一个按钮的「整理目的地介绍」「重试整理」状态采用相同布局。

保持文案、颜色、回调、加载状态和缓存行为不变。本次不调整其他按钮或页面结构；无需新组件、网络调用或新的业务测试。

验收：debug 构建与 lint；保留数据覆盖安装到 Mate 60 Pro；在已有泰安缓存页底部读取 UI 边界并截图，确认日期、更新文字及资料来源 x 坐标一致，按钮点击区域高度至少 48dp。验证时不点击更新，不重新生成内容，核对缓存与密钥配置校验值未变化。

继续使用功能分支和个人自用本地提交，版本保持 0.4.0 / 8。修改前源码备份在 `.verification-private/v0.4.0/guide-action-alignment/`，旧 APK 保留用于 `adb install -r` 回滚。更新最终验收记录并发送 punk-12 完成状态。
