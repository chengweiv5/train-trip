# 当前 UI 设计同步验证

状态：**DONE**。2026-09-19，基于主线 `d457523`，分支 `codex/detail-compact-dates`。本轮只同步设计与说明，没有修改 Android 源码、安装 APK 或推送代码。

## 已完成内容

三份 Pencil 画布均已编辑并回读，完整可恢复数据位于 [当前恢复快照](../../design/recovery/current-ui/README.md)，13 张参考图位于 [current-ui](../../design/reference/current-ui/)。两张旧未定稿单行预览已复制、校验 SHA-256 后归档到 `.verification-private/design-current-ui/archived-single-row/`，原目录不再作为交付材料。

- 首页：五个时段同一行、等宽、间距 6dp、横向内边距 4dp；普通按钮 48dp 高，大字体示例 52dp 高，标签完整。
- 车次：388dp 页面有 348dp 内容宽度，全部日期和 10月29–31日四个按钮总宽 318dp（72 + 76×3 + 6×3），剩余 30dp。320dp / 1.3 倍字体保持横向滚动，完整日期保存在可编辑图层中。
- 卡片：12dp / 10dp 内边距、6dp 行间距、10dp 卡片间距；普通双席别卡片 199dp 高。时刻 26sp，日期并入标题，查询时间和更多席别同一行。大字体页时刻双列、耗时下移、查询时间分两行；底部操作区不覆盖卡片。
- 交互：保留最新整卡标记、只读席别、无需选择即可打开 12306 App 和无复制入口。目的地四分页方向保持；旧车次页面标为历史。
- 文案：移除当前界面的旧查询范围警告、内部说明和重复提示；保留实际错误与重试。已确认画板移除“待确认 / 未进入开发”。

## 检查结果

| 文件画布 | 根节点 | 节点（含实例展开） | 异常裁切 | 同级重叠 | 未完成占位 | 无效引用 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| train-trip-v1 | 20 | 866 | 0 | 0 | 0 | 0 |
| train-trip-destination-ui | 29 | 1470 | 0 | 0 | 0 | 0 |
| train-trip-v0.4.0 | 7 | 355 | 0 | 0 | 0 | 0 |

车次大字体日期条的右侧两个按钮位于横滑视口边缘或外部，是预期滚动裁切；隐藏的旧第二卡片不参与可见验证。布局检查排除这两处预期情况，保留原始记录便于复核。比较前后原生节点，首版 10 个、目的地版 12 个、车次版 2 个无关根节点保持一致；改动根节点清单见 [结构记录](../../design/validation/current-ui.json)。

13 张参考图经宿主机 Vision OCR 识别 405 段文字，结合源文字和渲染图检查。普通车次页识别到全部日期、10月29日、10月30日、10月31日及卡片查询时间。OCR 会误识别图标与细字，不作为逐字准确性的唯一依据。[OCR 结果](../../design/validation/current-ui-ocr.json)。

## 原生保存与重新打开验证

三份文件均通过原生 Pen 窗口保存，保存后“Edited”标记消失；随后逐份切换离开并重新打开，确认窗口 URL 为本工作区路径，再通过 Pencil MCP 回读。之前的 Mac 锁屏阻塞已解除。

- 首版 20 个根节点、目的地版 29 个根节点及变量，与保存前验收快照完整一致。
- 车次稿先将恢复快照逐项应用到本工作区，再保存、切换、重开。7 个根画板 ID、全部文本和组件引用保持；355 个节点的实际位置和尺寸与验收版一致，差异 0。新增节点获得了新的原生 ID，恢复快照和结构记录已同步。
- 车次稿普通日期行仍为 318dp，卡片间距与内边距正确；重开后未完成占位 0、异常裁切 0、同级重叠 0、无效引用 0。两处大字体横滑边缘裁切为预期行为。
- 车次参考图已从保存后重开文件重新导出。全部恢复 JSON 与原生文件对应，原件及旧快照仍在私有备份目录。

最终磁盘 SHA-256（包含本轮设计变更）：

- 首版：`fb0a251669a211350f197eb243fffd1085411b1d75b85643e2a311bbdf96d841`（320439 字节）
- 目的地：`41a2d03dd8c3503f8901032d3fb0222f96f6f17c7853e2c55debdd02ce672b7f`（588291 字节）
- 车次：`e2e8cb41e52ae1962935c0730c5e9b5156d1c452544f6c6f4b73ca8561a278c5`（151468 字节）

## 备份与回滚

`.verification-private/design-current-ui/original/` 保留首版、目的地和早期车次原件；车次稿 rebase 后的新原件在 `latest-upstream/train-trip-v0.4.0.pen`，恢复时使用该份保留整卡标记版本。同目录含文档备份、MCP 修改前和当前快照、旧预览归档清单。rebase 前分支为 `codex/backup-before-design-sync-20260919`。

回滚前先检查并备份后续工作；只恢复本轮对应文件和文档，不整目录覆盖。当前无需回滚 App 或远端代码。
