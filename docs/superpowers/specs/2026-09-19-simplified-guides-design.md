# 目的地资料仅收录简体中文

用户明确要求不收录繁体资源；实际反馈城市为保定。保定缓存引用了人民网 `/BIG5/` 页面，景点正文、地址和来源标题保留了繁体原文。

处理原则：直接拒绝繁体资源，不通过繁简转换伪装为简体来源。Tavily 的 `zh-cn` 参数仅是检索偏好，不能代替本地校验。

1. 搜索阶段：国内域名规则之上拒绝 BIG5、zh-Hant、zh-TW、zh-HK 等繁体版本标识；标题、完整返回正文及图片说明包含繁体专用字形时排除。保留简繁共用字与合法人名用字。
2. 生成阶段：请求明确使用简体中文；送入模型前再次校验资料，解析后检查所有展示字段及证据/来源。引用仍必须逐字对应原始简体正文。
3. 缓存阶段：读取/保存均执行同一规则。旧缓存不改写为简体；不合格旧缓存不再展示，保留原文件可回滚。旧的尝试标记只允许一次按新规则重新整理，失败后保留显式重试，避免自动重复消费。
4. 识别数据：离线使用固定版本 OpenCC 官方字形词典派生的繁体专用字集合，保留许可证和来源；不引入网络识别服务。
5. 验证：以保定 BIG5 网址及野三坡原文复现；覆盖混合文本、简体共享字、模型输出、图片描述、缓存和失败后重启。真机保留设置与有效缓存覆盖安装。

搜索未找到合格资料时展示明确错误，绝不退回繁体资源。资源字形以取得的正文/元数据和语言版本 URL 校验；不改写第三方网页，也不对照片像素做文字改写。
