# v0.8.0 发布验证

2026-09-22，基线 `cd79824`，分支 `codex/release-v0.8.0`。本次只更新版本元数据和发布文档，业务源码保持已验证的条目相册及缓存保留实现，版本为 `0.8.0` / versionCode `13`。

## 构建与签名

- `:core:test :app:assembleRelease :app:lintRelease --offline --console=plain` 成功。核心114项，0失败/错误/跳过；Release lint 0错误、16项既有警告。
- `artifacts/train-trip-v0.8.0-release.apk`：8972841 字节，包名 `cn.traintrip.app`，Android 8.0+，Release 不可调试。
- SHA256：`23f410059ed2fb7ff23b0acd33dec898ccb6aff673f23ba6794d00c7d8ce1d45`。
- 正式证书SHA256：`a92fee2f6cce49f7bd091786a45c20768081eeef5d3b5f2e67e0544293ee1c17`，与重新下载的公开v0.7.0一致。v2/v3签名和zipalign验证通过。
- 签名包与未签名构建除签名元数据外逐项内容一致；服务Key、私钥、私有配置路径扫描0命中。密码从仓库外文件读取，不进入命令行、Git或附件。

## 升级验证

专用模拟器 `emulator-5586` 安装公开v0.7.0，下载APK摘要与其校验文件一致。使用纯离线夹具设置两座收藏城市、邯郸的广府古城和博物馆介绍、拽面及一张旧格式本地图，在旧版界面确认收藏和介绍可读后，覆盖升级正式签名v0.8.0。

覆盖安装Success，冷启动正常；appId与首次安装时间保持一致。收藏、介绍JSON、图片文件的SHA256升级前后完全相同；回拉安装APK摘要与发布包一致。新版想去页仍显示2城市，广府古城和博物馆可读，旧图正确迁移到广府古城条目下，美食缓存可读。没有输入真实服务Key或调用付费内容服务。

夹具图片复用内置离线样图，仅验证旧缓存读取及布局归属，不能作为真实广府古城照片证据。没有操作个人手机或其他任务的模拟器。

业务行为沿用同源码已通过的114项核心和33项Android回归，见[条目保留验收](2026-09-22-guide-refresh-retention.md)和[条目相册验收](2026-09-22-destination-item-photos.md)。本次没有重跑真实票源或付费搜索。

## 发布与回滚

中文更新说明在[发布说明](../releases/v0.8.0.md)，README下载入口与历史索引同步更新。APK和同名.sha256作为GitHub Release附件，APK不随源码提交。先验证草稿目标、正文、附件和摘要，再公开Latest并重新下载复验。

本地证据 `.verification-private/v0.8.0/`：`build.log`、`artifact-verified.json`、`upgrade/verified.json`；修改前备份 `before.tar`。

源代码和文档可反向提交。公开后不移动标签；应用问题通过同生产证书、更高版本号修复。旧版无法读取超过原数量上限的累计缓存，不建议降级或卸载清除数据。手机已安装的历史设备证书与公开证书不同，正式APK不能交叉覆盖。

## 公开后回读

[v0.8.0](https://github.com/chengweiv5/train-trip/releases/tag/v0.8.0) 已公开并设为 Latest，非草稿、非预发布；发布时间 2026-09-22 23:48:35（北京时间）。标签和发布时main均为 `98b619a4143651702673f8384e39cab54ee66c59`。中文正文和两项附件已回读，公开APK与SHA256重新下载后均与本地及GitHub摘要一致。专用模拟器已关闭。证据 `published-verified.json`、`latest.json`、`remote-refs.txt` 和 `published-download/`。
