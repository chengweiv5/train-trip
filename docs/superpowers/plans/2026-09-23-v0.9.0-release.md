# v0.9.0 发布计划

> **For agentic workers:** 沿用已验证的项目发布流程，在当前会话逐步执行。用户已授权发布；本环境无 superpowers 执行子技能。

**Goal:** 将已推送的豆包搜索与民间攻略实现发布为 v0.9.0 正式版。

**Architecture:** 从 56a9405 发布，只修改版本及发布资料。使用既有生产证书签名，在独立模拟器验证公开 v0.8.0 覆盖升级，核对草稿附件后公开 Latest。

**Tech Stack:** Kotlin、Gradle、JDK 21、Android SDK 36、apksigner、ADB、GitHub CLI。

## 约束

- 分支 codex/release-v0.9.0；版本 0.9.0 / 14；包名 cn.traintrip.app。
- 生产证书 SHA256 a92fee2f6cce49f7bd091786a45c20768081eeef5d3b5f2e67e0544293ee1c17。
- fetch 并确认快进后执行 `git push origin HEAD:main`，不强推、不移动公开标签。
- 不重跑付费查询，不操作个人手机，不批量生成城市内容。
- 私钥和个人 Key 不入包或入库；备份和中间证据放 .verification-private/v0.9.0/。

## Task 1: 版本与构建

修改 app/build.gradle.kts、README.md、docs/development-history.md；新增 docs/releases/v0.9.0.md、docs/verification/2026-09-23-v0.9.0-release.md、artifacts/train-trip-v0.9.0-release.apk.sha256。

- [x] 远端与 56a9405 一致；无开放 PR、v0.9 标签，上一公开版 v0.8.0。
- [x] 备份现有文件，更新 versionCode = 14、versionName = "0.9.0" 与中文说明，保留历史截图出处。
- [x] 执行 `./gradlew :core:test :app:assembleRelease :app:lintRelease --offline --console=plain`。
- [x] zipalign、生产签名、apksigner verify；核对版本、包名、不可调试、SHA256 和敏感数据。

## Task 2: 升级与发布

- [x] 下载并核验公开 v0.8.0 APK；独立模拟器设置离线收藏和介绍，覆盖安装 v0.9.0。
- [x] 对照文件摘要、UID/首次安装时间、界面和回拉 APK；关闭本任务模拟器。
- [x] 提交版本与验证资料，刷新远端并快进推送 main，回读提交。
- [x] 创建 v0.9.0 草稿，上传 APK 和 SHA256，检查正文、附件摘要和目标提交；公开 Latest。
- [x] 重新下载公开附件并核对摘要、签名、版本及 tag/main；保存发布验证记录。
- [x] 发送 punk-12 完成通知并回读。

## 回滚

版本和文档可以反向提交。公开后不移动标签；通过同生产证书、更高版本号修复应用问题，避免卸载清除数据。

发布提交 44f8368，已公开并重新下载核验；完成通知 om_x100b641f2e8804a0c1284f4d2542f01 已发送并回读。
