# v0.7.0 发布验证

日期：2026-09-22。基线 `da99ef5`，发布分支 `codex/release-v0.7.0`。版本提升为 `0.7.0` / versionCode `12`，汇总此前 v0.6.1 开发功能及已确认的想去省份侧栏、全部入口；本次不新增行为逻辑。

## 构建与签名

- `:core:test :app:assembleRelease :app:lintRelease --offline` 通过；核心测试 86 项，0 失败；Release lint 0 错误 / 18 项警告。
- `artifacts/train-trip-v0.7.0-release.apk`：8,956,457 字节，包名 `cn.traintrip.app`，名称「有票再出发」，不可调试。
- SHA256：`46cde042d78c140e355e84f2e95a118a51c258e12941625b395e0f04be21d862`。
- 沿用 v0.4.0 / v0.5.0 / v0.6.0 的生产证书：`a92fee2f6cce49f7bd091786a45c20768081eeef5d3b5f2e67e0544293ee1c17`；v2/v3 签名、zipalign 检查通过。
- 签名包与未签名 Release 构建逐项 ZIP 内容相符；服务 Key 模式、私有配置路径扫描 0 命中。签名私钥和密码保留在仓库外，不进入提交或附件。

## 升级验证

全新专用模拟器 `emulator-5586` 安装生产签名 v0.6.0，添加演示收藏安庆、蚌埠后，覆盖升级到本次 v0.7.0。

- 覆盖安装 `Success`，冷启动成功；应用显示新名称。
- 两座演示城市保留，默认「全部」显示 2 城市 / 1 省级地区；切换安徽省后仍正确显示两城。
- 两项内容服务仍为「未配置」，没有注入个人 Key 或调用付费服务。
- appId 和首次安装时间保持不变，回拉 APK 的 SHA256 与本地一致；Release 包保持不可调试。
- 想去页截图经视觉核对。专用模拟器已关闭，未操作个人真机或其他任务设备。

省份筛选、全部计数与排序、取消/撤销、窄屏菜单、各自滚动恢复和收藏持久化沿用同一功能源码已通过的 28 项离线界面回归。此前相册与检查历史验证见 [v0.6.1 验收](2026-09-21-v0.6.1.md)。本轮没有重跑真实票源或付费内容服务测试。

## 发布与回滚

更新说明保存在 [v0.7.0](../releases/v0.7.0.md)，README 下载入口和截图版本来源同步更新；现有可编辑 Pencil 文件随功能提交交付，历史画板标签保持原版本。

APK 与 `.sha256` 作为 GitHub Release 附件上传，APK 不随源码提交。发布前验证草稿附件和目标提交，公开后重新下载并核对摘要；外部读回证据保存于 `.verification-private/v0.7.0/`。

[v0.7.0 Release](https://github.com/chengweiv5/train-trip/releases/tag/v0.7.0) 已公开并设为 Latest，非草稿、非预发布；标签指向 `267163c13814091a117097df55dc3d82a07ca2a9`。正式 APK 和校验文件已重新下载，二者均与本地及 GitHub 附件摘要一致；中文正文已回读核对。代码和 Pencil 设计已快进推送 `origin/main`，完成通知已通过 punk-12 发出并回读。

代码和文档可通过反向提交回滚。公开后不移动版本标签，APK 问题使用同生产证书、更高版本号发布修复，不卸载或清除用户数据。历史设备专用证书与公开正式证书不同，不能直接交叉覆盖。
