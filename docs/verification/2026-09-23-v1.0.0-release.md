# v1.0.0 发布验证

2026-09-23，业务基线 `600c376`，发布分支 `codex/release-v1.0.0`。版本升级为 `1.0.0` / versionCode `15`；本轮修改版本、README 和发布资料，业务代码沿用已验证提交。

## 范围

四套主题和切换详见[主题验收](2026-09-23-v1.0-themes.md)，玩法修复详见[独立玩法验收](2026-09-23-independent-routes.md)。正式包包含两者，沿用既有生产证书。本轮未操作个人手机，未调用真实查票、搜索或模型服务。

## 构建与签名

- `:core:test :app:testDebugUnitTest :app:assembleRelease :app:lintRelease --offline --console=plain` 成功；146 项核心测试、6 项主题状态测试通过，0 失败/错误/跳过。
- Release lint 0 错误，9 项既有警告。主题功能此前 24 个独立 Android 用例通过，本轮在正式包上补验覆盖升级和原生主题切换。
- APK：9,021,993 字节，包名 `cn.traintrip.app`，版本 `1.0.0` / `15`，Android 8.0+，不可调试。
- SHA256：`c4432c90661b02f23ba5f5047c219a361f6bb8ee5a83ef5b77f85de97d5c09d0`。
- 生产证书 SHA256：`a92fee2f6cce49f7bd091786a45c20768081eeef5d3b5f2e67e0544293ee1c17`，与重新下载的公开 v0.9.0 一致；v2/v3 签名和 16 KB zipalign 校验通过。
- 签名前后除签名元数据外逐文件一致；包内私钥、密码文件、私有路径及服务 Key 模式检查无命中。签名密码通过仓库外文件参数读取。
- 原始证据：`.verification-private/v1.0.0/build.log`、`artifact-verified.json`、`signature.txt`、`zipalign.txt` 和 `previous-download/`。

## 正式包覆盖升级

专用 API 36 ARM64 模拟器 `emulator-5596` 安装重新下载并验证的公开 v0.9.0。建立两座收藏、3 人 / 2 小时 / 10月1–3日筛选、模型名称偏好，以及天津离线介绍、旧格式 1/2 日玩法和一张本地样图。旧版界面确认这些样例可读后，执行 `adb install -r --no-incremental` 覆盖安装生产 v1.0.0。

安装返回 Success，冷启动正常；appId 和首次安装时间不变。收藏、筛选、模型名称、离线 JSON、图片五个业务文件的 SHA256 升级前后相同。回拉已安装 APK 与候选逐字节一致；新版界面仍显示两座收藏、原筛选、离线文字、图片与旧玩法名称。

首次升级默认晴空蓝。通过原生「设置 → 外观 → 主题配色」选中松林绿，终止进程后冷启动首页为绿色；独立主题文件值为 `green`，业务文件继续不变。

图片来自项目内置资料，仅用于验证旧图片文件可保留与显示，样例关联不是景点照片匹配结论。未输入真实服务 Key，因此本轮未独立验证生产凭据解密。首次样例准备中缺少缓存字段、模拟器重装后安全标签不匹配，修正样例并确认旧版可读后重做完整升级；通过证据以 `upgrade/verified-*` 与 `upgrade/verified.json` 为准。

## 发布与回滚

[v1.0.0](https://github.com/chengweiv5/train-trip/releases/tag/v1.0.0) 已于北京时间 2026-09-23 21:55:43 公开，并设为 Latest，非草稿、非预发布。标签与发布时 main 均为 `2d16990e5fb9823e6a23b8e3250f86c82da59162`。

两项附件 `train-trip-v1.0.0-release.apk` 和 `.sha256` 已用 `gh release download` 重新下载，与本地逐字节相同；生产签名、包名、版本、不可调试状态再次校验通过。发布正文、GitHub 附件摘要、Latest 和远端标签均已回读。README 下载链接和更新说明已随发布提交更新。

证据为 `.verification-private/v1.0.0/published-verified.json`、`published-download/`、`published-readback.json`、`latest.json` 与 `remote-refs.txt`。专用模拟器已关闭；后续文档提交只补充本段证据，不移动发布标签。

修改前备份为 `.verification-private/v1.0.0/before.tar`。代码或文档使用反向提交恢复；公开后不移动标签，通过同生产证书、更高 versionCode 的修复版处理。v0.9.0 不支持全部新路线字段，不直接降级或宣称旧包可完整读取新缓存。历史设备证书与生产证书不同，不交叉覆盖，不卸载清数据。
