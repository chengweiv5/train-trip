# v0.6.0 真机安装与代码交付

日期：2026-09-21。功能分支：`codex/v0.6.0-b4-ui`。构建源码：`6e0fb921782fa1ed1d25bd11b513cd2d434afe0d`。版本：`0.6.0` / versionCode `10`。

## 范围

本次交付包含 B4 冰蓝湖蓝 UI、齿轮设置图标、更新说明链接修复，以及出发时段快捷多选、最长车程 3/5 小时与自定义滚轮、目的地选择的置顶“想去”分组。代码、测试与可编辑设计随功能提交一起推送到 `origin/main`；本次不创建 GitHub Release。

推送前已刷新远端，开放 PR 为 0，`origin/main` 是构建源码的祖先，本地领先 6 个提交、落后 0 个，无需 rebase。补充本安装记录后再次刷新并核对快进关系，使用 `git push origin HEAD:main` 交付，推送结果在本地私有记录及任务状态中回读留证。

## 构建和包验证

JDK 21 下重新执行 `:app:assembleRelease` 成功。构建元数据为 Release；v2/v3 签名、zipalign、版本和包名检查通过，`debuggable=false`。包内服务 Key 模式和私有配置文件扫描均为 0 命中。

| 用途 | 文件 | SHA256 |
| --- | --- | --- |
| 生产签名候选 | `artifacts/train-trip-v0.6.0-release.apk` | `f8f7affef16ac7addde26aee175f7997511f4752feae2858000132e88191461b` |
| 真机保留数据升级 | `artifacts/train-trip-v0.6.0-device-release.apk` | `635700fbb65c7a8416708908833d74e081e55846db1390f6d957bc5c3183d848` |

两包各 8,923,689 字节，签名以外的全部 ZIP 内容与同一未签名 Release 构建逐文件一致。生产候选沿用历史正式发布证书；手机包沿用设备原有的历史 Debug 证书，但构建类型仍为 Release、不可调试。两种证书不同，不可互相覆盖升级。

本次没有修改生产代码，沿用已完成的离线验收：83 项核心测试；时段多选 16 项相关 Android 回归；车程选择器 10 项及窄屏复测；想去分组 12 项及窄屏复测。最近一次完整 lint 为 0 错误、19 条既有警告。详情见[时段多选](2026-09-21-departure-time-multi-select.md)、[车程选择器](2026-09-21-duration-picker.md)、[想去分组](2026-09-21-wish-destinations.md)。

## 真机安装

- Mate 60 Pro / ALN-AL00 在线，安装前已备份原 APK 和包状态，核对新旧设备证书一致。
- `adb install -r --no-incremental` 返回 `Success`，未卸载、未清理应用数据。
- 回拉安装后 APK，其 SHA256 与待安装设备包一致；包状态为 `0.6.0` / `10`、不可调试。
- UID 和首次安装时间保持不变。没有读取个人密钥、收藏内容或缓存，也没有启动 App、触控界面或调用付费内容服务。
- 本次只验证安装结果；不将既有模拟器界面验收描述为本次真机界面验收。任务栈快照未提供可比较的前台标记，未声称独立验证前台保持不变。

## 设计、证据与回滚

`design/train-trip-surface-contrast.pen` 已包含 08 时段多选、09 车程滚轮和 10 想去分组，SHA256 为 `c24165ca819deabc536893275d3f3ecf5fd7e429fc0c57ecc8afb206580cbc10`。本次未再次编辑设计。

构建、签名、扫描、安装和推送证据，以及旧 APK、旧候选和修改前文档，保存于忽略目录 `.verification-private/v0.6.0/install-push/`。APK 不进入 Git，校验文件随源码提交；非本任务的 `.codex/config.toml` 不纳入。

原手机 APK 为同证书、同 `0.6.0` / `10`，保存在上述目录的 `before.apk`；需要时可保留数据覆盖回退。旧版读取快捷多选时会退化为保留的最早时段。源码或设计回滚采用指定功能提交的反向提交，保留后续变更，不重置或强推远端。
