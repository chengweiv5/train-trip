# 当前 UI 设计同步

按用户已确认的现有实现同步可编辑设计，不重新设计交互。执行期间主线更新至 `d457523`：保留新增整卡标记车次、无需选择即可打开 12306 及目的地四分页。本任务只补齐既有布局和文案；图文来源与四分页专门稿由对应任务负责。

## 范围

- `design/train-trip-v1.pen` 与 `design/train-trip-destination-ui.pen`：首页五个出发时段固定单行、等宽、6dp 间距、4dp 横向内边距，至少 48dp 点击高度。普通字号和 320dp / 1.3 倍字体保留完整标签；更新开发交接，移除旧换行约定。
- 目的地版结果、选择器、筛选弹层和资料页：移除首版查询范围提示、内部字段解释及重复引导；同步“查询完成”、实际失败/未开售/待查询、简短日期摘要，保留错误原因和有用规则。首版对应公共界面同步，历史购票流程明确标为历史；现行车次交互以 v0.4.0 为准。
- `design/train-trip-v0.4.0.pen`：日期条“全部日期 + 三日期”普通宽度完整单行；较多日期横向滚动。卡片内边距横向 12dp/纵向 10dp、行间距 6dp、卡片间距 10dp，时刻 26sp。出发日期移到车次号行；查询时间与更多席别同一行，大字体时间可分行。保留最新主线的整卡标记、只读席别、无需选择即可打开 12306、无复制入口和完整行程摘要。
- 更新 README 与当前设计说明，导出最终参考图和验证记录；两张旧单行临时预览归档本地备份，避免作为正式设计交付。

## 验收与回滚

保留现有配色、字体、可编辑图层及无关画板。通过 Pencil MCP 修改；不直接解析或改写 `.pen`。修改前备份，逐页检查实际边界、裁切、同级重叠、占位状态及引用。普通宽度约 388dp / 字体 1.0，使用 10月29日、10月30日、10月31日验证“全部日期 + 三日期”完整显示。320dp / 1.3 倍字体允许日期条横向滚动、卡片顶部及底部文字换行。普通/大字体截图与 OCR 复核；保存后重新打开并回读关键节点，确认落盘。无关根节点内容保持。更新当前恢复快照，旧恢复材料明确标为历史版本。

备份目录 `.verification-private/design-current-ui/original/`。回滚只恢复本轮修改的设计文件及说明，恢复前检查是否存在后续修改。本轮不改 Android 代码、查询规则或已安装 APK。
