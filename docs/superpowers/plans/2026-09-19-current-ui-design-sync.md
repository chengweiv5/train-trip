# 当前 UI 设计同步执行计划

**Goal:** 将三份可编辑稿与 `d457523` 的已确认界面对齐，保留整卡标记与目的地四分页。

**Architecture:** 延用现有画板与组件，按单文件顺序编辑、检查和保存；当前车次交互由 v0.4.0 稿表达，公共筛选/结果界面同步到对应稿。

**Tech Stack:** Pencil MCP、既有 `.pen`、Markdown、系统 OCR。

## 约束

- 不改 Android 代码和业务逻辑，不操作其他任务的工作区文件。
- `.pen` 只通过 Pencil MCP 读取和修改，二进制复制仅作备份。
- 普通和大字体预设单行；日期长列表可横滑；交互控件保留 48dp 点击目标，席别为只读文字。
- 修改根画板时设置 placeholder，检查完成立即解除；最终保存并回读。

## 步骤

- [x] 刷新主线、检查待合并 PR，备份三份 `.pen` 与说明，确认其他设计任务空闲。
- [x] 通过 Pencil 读取首版、目的地版和 v0.4.0 对应节点、文本及布局，列出实际修改映射。
- [x] 首版和目的地版更新单行预设、公共旧文案与交接说明；逐页检查边界、导出参考图，保存并回读。
- [x] v0.4.0 更新三日期单行、卡片密度、旧提示和普通/大字体示例；检查边界、导出、保存并回读。
- [x] 同步 `design/README.md` 与相关说明，记录节点、尺寸、截图/OCR、落盘和无关内容验证。
- [x] 将两张旧临时预览归档到私有备份，更新任务状态，发送完成通知。

当前进度：三份原生设计保存、重新打开回读、当前恢复快照、13 张参考图、OCR 和结构检查已完成。旧预览已校验归档，Mac 锁屏阻塞已解除。
