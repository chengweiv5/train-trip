# v0.8.0 发布计划

> **For agentic workers:** 在当前会话按步骤执行；本机未提供 superpowers 执行子技能。用户已授权发布，无需再次确认。

**Goal:** 发布包含条目相册、3/100 图片上限和刷新保留缓存修复的 v0.8.0 正式版。

**Architecture:** 从已推送并验证的 cd79824 发布；只更新版本元数据和发布文档，不改变业务逻辑。沿用公开正式版生产证书，在专用模拟器验证 v0.7.0 覆盖升级；先核验草稿附件，再公开为 Latest。

**Tech Stack:** Kotlin、Gradle、JDK 21、Android SDK 36、apksigner、ADB、GitHub CLI。

## 约束

- 功能分支 `codex/release-v0.8.0`，推送使用 `git push origin HEAD:main`，先刷新主线并检查快进，不强推。
- `app/build.gradle.kts` 版本改为 `versionCode = 13` / `versionName = "0.8.0"`。
- 正式证书 SHA256 为 `a92fee2f6cce49f7bd091786a45c20768081eeef5d3b5f2e67e0544293ee1c17`，设备专用证书不用于公开附件。
- 每景点/美食最多 3 图、每城市最多 100 图；只补零图条目；全量更新保留全部旧条目、更新同项、追加新增。
- 不调用真实模型或搜索，不改个人手机；已有核心114项和Android33项回归可复用，发布包另验证构建、签名和正式升级。
- 私钥、密码和个人配置不进入Git或Release附件。备份与日志放在 `.verification-private/v0.8.0/`。

## Task 1: 发布元数据与构建

Files: 修改 `app/build.gradle.kts`、`README.md`、`docs/development-history.md`；新增 `docs/releases/v0.8.0.md`、`docs/verification/2026-09-22-v0.8.0-release.md`、`artifacts/train-trip-v0.8.0-release.apk.sha256`。

- [x] 刷新 origin/main，确认基线 cd79824；开放 PR 为0，v0.8.0无现有标签或Release，最新公开版v0.7.0。
- [x] 更新0.8.0/13、中文更新说明和下载入口；截图保持历史出处，不宣称是新截图。
- [x] `./gradlew :core:test :app:assembleRelease :app:lintRelease --offline --console=plain` 全部通过。
- [x] `zipalign` 后使用仓库外正式私钥签名；`apksigner verify --verbose --print-certs`，核对应用ID、版本、不可调试和证书；生成SHA256文件。
- [x] 核验包内无私有配置，已签名与未签名包内容除签名元数据外一致。

## Task 2: 升级与发布

- [x] 下载公开v0.7.0 APK和SHA256并核验；专用模拟器安装旧版并准备演示收藏、离线介绍。
- [x] 覆盖升级0.8.0，验证数据和旧图保留、UID/首次安装时间相同、启动成功、版本正确、回拉APK摘要一致；仅关闭本任务模拟器。
- [x] 提交版本、说明、验收和校验文件；刷新远端、确认快进后推送并回读。
- [x] 创建指向已推送发布提交的v0.8.0草稿，上传正式APK和SHA256；回读草稿正文、附件摘要、标签目标后公开Latest。
- [x] 用 `gh release download` 重新下载公开附件，核对哈希和中文说明、非草稿/非预发布/Latest、远端标签和main。
- [x] 更新验收与状态，发送punk-12完成通知并回读。

## 回滚

版本和文档可反向提交；公开后不移动标签。已安装的新版本通过同生产证书、更高版本号修复，避免卸载清除用户数据。旧版本无法读取超出旧条目数量上限的累计缓存，不建议降级。

公开发布与附件复验完成；标签98b619a，完成通知 om_x100b641e71ba08a4b10bb3e790a44ce 已发送并回读。
