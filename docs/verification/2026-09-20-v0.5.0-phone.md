# v0.5.0 真机覆盖安装记录

2026-09-20，用户通知真机已连接，基于已验证实现 `e1f3bf8` 安装到 Mate 60 Pro（ALN-AL00）。

## 安装与签名

手机原版本为 v0.4.0 / versionCode 8，使用历史 Debug 证书。为保留个人数据，使用同一 Release 构建、原设备证书生成 `artifacts/train-trip-v0.5.0-device-release.apk`，没有卸载或清除数据。此包是 Release 构建、`debuggable=false`，但不是公开发布所用的生产证书。

公开包 `artifacts/train-trip-v0.5.0-release.apk` 未改变，继续使用生产证书。两种签名的安装包不能直接互相覆盖。

- 真机包版本：0.5.0 / 9。
- 真机包 SHA256：`caa5e7c45c637077dd11c1c9723a7852da1d529610add0e1a01a35238150f408`。
- 设备证书 SHA256：`cae9ed8a5c544fc2088161792d4811d60ba988265d76b476d3857ab716b6c603`，与安装前一致。
- v2/v3 签名检查成功；包内服务 Key 模式扫描无命中。
- `adb install -r --no-incremental` 成功；回拉已安装 APK 后哈希与本地一致。
- 冷启动 `Status: ok`，422 ms；系统包信息确认为 v0.5.0 / 9，无 DEBUGGABLE 标记。

## 数据及验证边界

安装前回拉旧 APK，备份私有目录 files/shared_prefs 共12个文件，包括筛选、两项加密配置和城市资料。备份内容未提交Git，未打印密钥。

安装后首页确认原有 10月1–3日、5人、3小时、二等座/一等座条件仍在。随后准备核对设置时，手机前台已切回飞书，一次点击落在飞书菜单，立即停止触控；未发送消息或修改飞书业务资料。用户中断后设备断开，未继续操作界面。

因此本次确认安装、签名、包哈希、启动和首页筛选保留；密钥解密状态、已有缓存及新增入口未完成真机界面复核。相关功能的模拟器验证见 [v0.5.0 验收](2026-09-20-v0.5.0.md)。

原始证据、旧 APK、私有备份及清单位于 `.verification-private/v0.5.0/phone/`，汇总 `final-verification.json`。如需回退，使用同设备签名且更高 versionCode 的修复包，保留数据；不卸载、不清除个人数据。本轮没有推送或发布。
