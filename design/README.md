# Train Trip V1 UI 设计

## v0.6.0 B4 冰蓝湖蓝

已确认并实现白底浅蓝卡片、首页与想去统一顶部；当前视觉以 [B4 设计说明](surface-contrast.md) 和 [Pencil 源文件](train-trip-surface-contrast.pen) 的07画板为准，08画板补充出发时段多选状态和交互规则。以下旧配色记录保留为历史。

出发时段四个快捷段可多选，自定义完成后替换为一个连续区间。08画板 `ghTAc` 中的首页 `JnJJH` 展示“早上＋晚上”，保留B4顶部与卡片样式；已完成保存、独立副本回读、结构及运行界面验证，见 [时段多选验收](../docs/verification/2026-09-21-departure-time-multi-select.md)。

## v0.4.0 设置菜单（2026-09-19）

[可编辑源文件](train-trip-v0.4.0.pen) 已保存四张新画板：设置菜单 `kL4Hp`、大模型设置 `Y1162`、搜索引擎设置 `cqFPn`、首页入口 `w27DL`。首页右上角进入设置，DeepSeek 模型/API Key 与 Tavily API Key 分项管理；保存只更新配置，空 Key 保留现有密钥。

原生保存、MCP 回读、无裁剪检查及 OCR/视觉复核已完成；Android 实现、模拟器和 Mate 60 Pro 验证见 [设置菜单验收](../docs/verification/2026-09-19-settings-menu.md)。

## 当前界面设计同步（2026-09-19，已完成）

三份 Pencil 源文件已补齐当前布局与文案，完成原生保存、重新打开回读、结构和渲染检查。首版和目的地版完整图层与验收快照一致；车次版恢复后保留所有原文、根画板和组件引用，全部节点尺寸与验收版一致。下列快照与参考图对应已保存文件。

- 首页五个出发时段固定单行、等宽，间距 6dp、横向内边距 4dp，点击高度至少 48dp；320dp / 1.3 倍字体保留完整标签。
- 车次页普通示例改为 388dp；“全部日期 + 10月29日 + 10月30日 + 10月31日”占 318dp，可用宽度 348dp。更多日期或大字体保持单行横滑。
- 车次卡片内边距横向 12dp / 纵向 10dp、行间距 6dp、列表间距 10dp，普通示例卡高 199dp。出发日期并入车次行，查询时间与更多席别同行；大字体查询时间分两行。整卡标记、只读余票和始终可用的 12306 App 按钮保留。
- 删除当前界面的旧范围提示、内部字段说明和重复引导；保留实际失败、待查询、未开售及重试信息。历史车次稿明确标注由 v0.4.0 替代。

[恢复快照](recovery/current-ui/README.md) · [参考图](reference/current-ui/) · [结构检查](validation/current-ui.json) · [OCR](validation/current-ui-ocr.json) · [本轮验证与保存状态](../docs/verification/2026-09-19-current-ui-design-sync.md)。目的地四分页与最新主线保持一致，历史目的地长页不作为当前开发依据。

## 目的地四分页（2026-09-19）

用户已确认景点 / 美食 / 玩法 / 贴士分页样稿，现已实现于 Android。景点和美食共用白色独立卡片，顶部紧凑摘要、主题切换与固定车次操作；窄屏或大字体时摘要进入滚动区域。可编辑样稿片段见 [approved-preview.html](reference/destination-pages/approved-preview.html)，完整约定见 [设计规格](../docs/superpowers/specs/2026-09-19-destination-pages-design.md)，运行验证见 [验收](../docs/verification/2026-09-19-destination-pages.md)。历史 Pencil 目的地长页保留为旧稿。

## v0.4.0 车次信息页 UI（2026-09-19）

[可编辑源文件](train-trip-v0.4.0.pen) · [完整设计说明](../docs/superpowers/specs/2026-09-19-v0.4.0-train-detail-ui-design.md) · [设计评审 PDF](reference/v0.4.0/export.pdf)

UI 设计已在 v0.4.0 App 中实现；运行验证见 [实现验收](../docs/verification/2026-09-19-v0.4.0-implementation.md) 和 [整卡选择验收](../docs/verification/2026-09-19-trip-card-selection.md)。整卡点击临时标记车次，席别只显示余票。底部有选择时显示车次摘要，“打开 12306 App”始终可用，点击唤起铁路12306；未安装或打开失败时提示并保留选择，不自动跳转官网。复制入口已移除，刷新余票放在列表上方。未选择、已选择、320dp / 1.3 倍字体及五张反馈卡片均已出稿。完整可编辑层级另存为 [恢复快照](v0.4.0-recovery.json)。原首版和目的地设计稿保留；下文描述历史版本，其强制购票前核验和官网跳转规则已由本节所链接的新设计和实现替代。

画板：未选择 `zvir8`、已选择 `bArbH`、大字体 `VhZVb`、反馈状态 `ePGKM`。结构、OCR 和视觉检查见 [验证记录](../docs/verification/2026-09-19-v0.4.0-train-detail-ui.md)。

## 凌晨出发预设（2026-09-19）

已同步 [目的地版](train-trip-destination-ui.pen) 与 [首版稿](train-trip-v1.pen) 的“几点出发”：全天、凌晨、早上、下午、晚上。凌晨对应 00:00–06:00，开始包含、结束不包含；点击立即应用并保存，具体范围在按钮下方显示。

本节记录最初补齐凌晨的已保存版本；其中最小宽 64dp、8dp 间距与窄屏换行已被本页顶部的单行规则取代。320dp、1.3 倍字体示例仍使用目的地版节点 `xl7od`、首版节点 `ATqc3`，开发交接节点为 `gNcl5`。本轮单行画布已保存并回读。

两份源文件均已在 Pencil 保存并回读；本次变更节点无裁切、同级重叠或未完成占位。参考图和 OCR 已复核，见 [凌晨选中示例](reference/early-morning/destination/xl7od.jpeg)、[最新首页](reference/early-morning/destination/a9i4j.jpeg) 与 [验证记录](validation/early-morning.json)。

本轮改动和 rebase 前状态均有备份，路径记录在本工作区 `.verification-private/rebase-backup-path`。若仅撤回凌晨设计，首版稿应使用备份目录中的 `origin-main-train-trip-v1.pen`，保留新主线画板；恢复前先备份后续修改。早期原件 `.verification-private/early-morning-design/original/` 基于旧主线，不应直接覆盖当前首版稿。


## 目的地省市分组 UI（2026-09-19）

[查看三屏预览](reference/destination-ui/review-overview.jpeg) · [同省展开/收起细节](reference/destination-ui/results-clarity-review.jpeg) · [完整结果页](reference/destination-ui/results-accordion-review.jpeg) · [完整设计说明](destination-ui-proposal.md)。结果页以连续省份容器包住城市卡片，明确显示“展开 / 收起”，收起时预览城市名称。默认展开第一个省份，其余收起，各省独立开合。整体 UI 已确认，并在 Android 0.2.0 中实现、验证及覆盖安装至 Mate 60 Pro；详见 [开发与真机验证](../docs/verification/2026-09-19-destination-provinces.md)。

省份分组版本已保存为 [train-trip-destination-ui.pen](train-trip-destination-ui.pen)，[当时的图层恢复快照](destination-ui-clarity-recovery.json)和预览保留为历史记录。本轮最新快照见顶部同步记录。原首版设计文件保留。

2026-09-18 完成首版可编辑 UI 设计。源文件为 [train-trip-v1.pen](train-trip-v1.pen)，使用 Pencil 打开与编辑。

本目录保存可编辑 UI 设计稿和交互说明。Android 首版已实现，运行验证见项目 README。画板中的日期、城市、车次、票数及查询时间均为设计示例，不代表实时余票。

## 2026-09-19 界面修复

- 出发时段 `HceGu`：同窗口显示开始/结束两组小时和分钟滚轮，点击“完成”一次应用；取消保留原值。支持跨午夜、恢复全天，结束为 24 点时分钟固定为 00；相等时不能应用。`NE7Tn` 展示窄屏或字体超过 1.15 倍时的上下布局。
- 时段窗口开发交接 `gNcl5` 已同步，参考图见 [time-picker](reference/time-picker)，验证见 [time-picker.json](validation/time-picker.json) 和 [时段选择验收](../docs/verification/2026-09-19-time-range-picker.md)。
- 日期面板 `u9YBQY`：同一日历选择起止日期、连续浅绿高亮、跨月滚动、取消/完成。未选完整不能应用，最多 31 天。
- 查询状态 `XCFLz`：6dp 连续轨道按已处理查询项推进，无分段间隙、无末端圆点。初始化显示空轨道与连接说明。
- 车次组件 `tPTGk`：居中紧凑耗时、独立实线、到达日期；实例同步。`QKZt6` 展示窄屏或字体超过 1.15 倍时，将耗时与到达说明移到时刻下方。
- 开发交接 `gNcl5` 同步上述交互和布局规则。参考图见 [ui-polish](reference/ui-polish)，验证见 [ui-polish.json](validation/ui-polish.json)。

## 画板导航

画布由上往下排列，前三张主页面横向并排。长页面展示完整内容，Android 实现时采用滚动布局。

| 画板 | Pencil 节点 | 内容 |
| --- | --- | --- |
| 设计说明 | `bi8Au` | 目标、主流程、示例数据声明 |
| 组件与样式 | `p7g9o` | 主按钮、次按钮、筛选标签及样式说明 |
| 01 出行条件 | `a9i4j` | 北京、单日或连续日期、时段、席别、人数、最长车程 |
| 02 有票城市 | `z1NQD` | 目的地城市、去重车次数、最短车程、查询覆盖、排序、刷新 |
| 03 车次详情 | `g7j1ms` | 日期、站点、发到时间、历时、席别余票、核验和 12306 入口 |
| 04 筛选弹层 | `FMp8K` | 出发时段、席别多选、北京同城车站 |
| 05 辅助选择 | `xyVsi` | 日期范围、成人数量、车程上限、席别详情 |
| 06 查询与异常 | `zu5eP` | 查询中、部分结果、无匹配、网络失败、未开售、核验失败、余票变化、多人数量待确认、停止查询 |
| 07 交互与开发交接 | `gNcl5` | 应用与取消筛选、返回、排序、查询状态、购票核验、Android 适配规则 |

右侧独立组件：城市卡片 `mH2BD`、车次卡片 `tPTGk`。总计 5 个可复用组件、35 个实例，所有实例引用有效。图层、文本、图标、变量及组件仍可编辑。

## 视觉与实现基准

- 手机页面以 412 逻辑单位宽度设计，边距 20，卡片圆角 16–22。
- 中文字体 Noto Sans SC；标题 28，正文 14–16，辅助信息 12–13。Android 用 sp 并允许字体放大。
- 主按钮高 56，交互控件目标点击区域至少 48dp；列表行和卡片整体可点击。
- 页面保留顶部 62、底部 28 的安全区示意。实现需使用系统实际 Insets，不硬编码设计值。
- 详情页购票操作在实现时固定于导航安全区上方，车次列表独立滚动。
- 目标设备为 Huawei Mate 60 Pro / HarmonyOS 4.2.0，计划 Kotlin + Jetpack Compose Android APK。

| 变量 | 值 | 用途 |
| --- | --- | --- |
| `tt-bg` | `#F5F3EE` | 页面底色 |
| `tt-surface` | `#FFFFFF` | 卡片 |
| `tt-ink` | `#20392C` | 正文 |
| `tt-muted` | `#617065` | 辅助文字 |
| `tt-primary` | `#2D5E3A` | 主操作 |
| `tt-tint` | `#E6EFDF` | 选中与轻背景 |
| `tt-line` | `#D8DED3` | 分隔线 |
| `tt-warning` | `#87531F` | 警示文字 |
| `tt-warning-bg` | `#FBF0DE` | 警示背景 |

## 首版历史交互约定（当前实现以顶部同步说明为准）

1. 出发地默认北京，合并同城车站；首版只查成人、直达、去程。默认 1 人，席别和时长不限，包括无座。
2. 日期或筛选弹层点“完成”应用；关闭弹层保留原值。日期两端包含，时段按所选每一天的北京时间生效。
   时段两端在一个窗口中直接滚轮选择，开始包含、结束不包含；00:00–24:00 表示全天。窗口只修改草稿，滚轮停止后才能应用，重建恢复草稿。窄屏或大字体时内容可滚动，底部取消/完成保持可达。
3. 城市按最短车程排序，可切换车次数最多；详情默认出发时间升序。返回保留筛选与列表位置。
4. 查询进度与结果分开保存。停止查询保留已查结果；继续只补未完成部分。进度按已处理查询项 / 总项展示，成功、失败与未开售分别计数。未核实全国覆盖时显示“可能未覆盖全部目的地”。
5. 城市车次数按乘客上车日期和内部列车编号去重，同车次不同到达站保留为不同乘车选项。
6. 多人不能跨席别合计余票。“有”但未返回数量时，1 人可以匹配；多人单独标记数量待确认，不计入确定满足人数的结果。
7. 候补、无票、未提供席别、未开售、查询失败和取消各自显示；不能统一处理成无票。
8. 前往 12306 前重新核验选中车次和席别。数量不足取消选择；失败保留旧时间并提供重试和手动查入口。未验证的跳转不能声称已预填条件。
9. 跨日到达保留 +1/+2 天；不根据时刻先后误算历时。购票、登录和支付在 12306 完成。

## 首版验证记录（2026-09-18）

- Pencil MCP 回读：648 个可见节点、293 个文字节点；没有空名称或空文字，没有未完成占位画板。
- 结构与坐标检查：未发现裁切、顶层画板相交或同级图层相交，重叠检查容差 0.5 逻辑像素；35 个组件实例均可解析。
- 最终视觉检查：三个主页面、筛选与日期面板、九种状态及交互说明均已通过 Pencil 渲染检查。
- 本机 macOS Vision 对 7 张最终参考图完成中英文 OCR，识别出 293 段文字。OCR 中图标和少量细字有误识别，已结合可编辑原文与渲染画面复核；不把 OCR 当作逐字无误的证明。
- 颜色对比度：正文/底色 11.25:1，辅助文字/底色 4.72:1，按钮白字/深绿 7.57:1，深绿/浅绿 6.41:1，警示文字/背景 5.66:1。
- 设计文件保存后通过文件元数据和 Pencil 当前文档回读确认。没有进行真机安装、字体放大、返回手势或真实点击链路测试，这些属于实现阶段验收。

结构结果见 [validation/layout.json](validation/layout.json)，OCR 原始结果见 [validation/ocr.json](validation/ocr.json)。[reference](reference) 内的 JPEG 是最终开发对照图，不替代可编辑源文件。

## 保留与回滚

原始空白画板以隐藏节点 `h0pju`（“备份 · 原始空白画板”）保留在源文件中。可在 Pencil 中将它启用并复制到新文档，恢复原空白画板；现有设计不必删除。当前界面修复在功能分支 `codex/ui-polish`，实现和对应设计稿同步管理。每轮修改前的备份及回滚方法见对应验收记录。

## 2026-09-19 目的地灵感（0.2.0）

本轮只覆盖天津、济南、青岛、大同、洛阳。新增画板保持原有深绿和暖白视觉，原车次卡片继续用于其他城市。

- 灵感卡片 `Mav9U`：离线实景图、一句话理由、标签、天数建议、车票摘要以及“了解目的地 / 查看车次”两个入口。
- 目的地详情 `syZ4p`：天津完整长页，景点、美食、一日玩法、季节和接驳提醒，底部查看车次在 App 内固定。
- 交互与来源 `e3qHZk`：五城范围、两条返回路径、离线和无资料降级规则。
- 新结果页 `J9IQLh`：灵感卡片与普通车次卡片混排，票数和时间均为设计示例。
- 来源弹窗 `pVPBz`：来源核对日期、正文许可、摄影作者和原图/许可入口。

新画板 79 个节点完成裁切、同级重叠、空文本/名称检查，问题为 0；新增根画板互不相交。Pencil 已保存并回读，图片仍由相对资源路径引用，文字、布局及组件均可编辑。5 张设计参考图和 7 张 App 运行图共完成 230 段 OCR，并结合源文字与实际画面复核。

[结构验证](validation/destination-inspiration.json)、[设计参考](reference/destination-inspiration)、[功能验收](../docs/verification/2026-09-19-destination-inspiration.md)。
