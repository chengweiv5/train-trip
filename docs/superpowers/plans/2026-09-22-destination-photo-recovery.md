# 目的地全量更新按需补图实施记录

基线 `8817acd`，功能分支 `codex/destination-photo-recovery`。最终范围按用户最新要求调整，旧独立补图界面已移除。

- [x] 实现城市/景点结构化图源、Tavily 定向备用、失败与取消处理。
- [x] 验证邯郸、保定、承德真实图片下载与解码。
- [x] 修正公开页面 UA 和照片 10 MiB 上限，继续校验重定向、来源与解码尺寸。
- [x] 统一全量更新：新文字与旧可用相册原子保存；有图跳过图片来源和下载，无图才补图。
- [x] 移除独立补图按钮、回调和独立页面状态；进度使用原整理状态区。
- [x] 更新核心/Android 回归测试、普通与大字体界面测试。
- [x] 同步可编辑 Pencil 设计，原生保存、独立副本回读与 OCR/视觉检查。
- [x] 最终构建、模拟器回归通过，更新验收记录。
- [x] 提交最终改动，发送 punk-12 完成通知并读回。

测试以 `:core:test :app:assembleDebug :app:assembleDebugAndroidTest :app:lintDebug --offline` 和专用只读模拟器运行。不更改版本、不推送、不发布、不安装用户手机。
