# v0.9.0 发布验证

2026-09-23，业务基线 56a9405，发布分支 codex/release-v0.9.0。版本为 0.9.0 / versionCode 14，本轮只修改版本和发布资料。

## 范围

已有功能验证见[民间攻略搜索验收](2026-09-23-community-search.md)：132 项核心测试通过，Android 14 个相关用例合计 13 项通过、1 项滚动位置断言在旧代码也失败。真实邯郸样本取得 4 景点、3 美食、2 条原文路线，12 张可解码图片覆盖 4/4 景点和 2/3 美食，不代表每城都有图。

本次沿用公开生产证书，不操作个人手机、不重复付费搜索或批量生成缓存。

## 回滚

修改前备份在 .verification-private/v0.9.0/before.tar；源代码和文档可反向提交。公开后不移动标签，通过同生产证书、更高版本号修复。历史设备包证书与公开生产包不同，不交叉覆盖、不卸载清数据。

## 构建与签名

- `:core:test :app:assembleRelease :app:lintRelease --offline --console=plain` 成功。132 项核心测试，0 失败/错误/跳过；Release lint 0 错误，16 项既有警告。
- 生产 APK：8989225 字节，包名 `cn.traintrip.app`，Android 8.0+，版本 `0.9.0` / `14`，不可调试。
- SHA256：`94b520947f15c44289c47bd3a7b951f35cc1239c8303aa09e9e711db3a565cbc`。
- 正式证书 SHA256：`a92fee2f6cce49f7bd091786a45c20768081eeef5d3b5f2e67e0544293ee1c17`，与重新下载的公开 v0.8.0 一致；v2/v3 签名和 zipalign 校验通过。
- 签名与未签名构建除签名元数据外逐项内容一致；包内个人 Key、私有路径及签名文件检查无命中。密码只通过仓库外文件读取。
- 证据在 `.verification-private/v0.9.0/`：`build.log`、`artifact-verified.json`、`signature.txt` 和 `previous-download/`。

## 正式包覆盖升级

专用模拟器 emulator-5590 安装重新下载并核验的公开 v0.8.0，设置两座收藏城市、邯郸离线介绍、一张本地样图及模型偏好。旧版界面确认收藏、介绍和图片可读后，以生产签名 v0.9.0 覆盖升级。

安装 Success，冷启动正常；appId、首次安装时间保持不变。收藏 JSON、介绍 JSON、图片及模型偏好文件 SHA256 升级前后一致；回拉已安装 APK 摘要与发布候选一致。新版收藏仍为 2 城，景点、美食、玩法和原文入口可读，旧图可显示。

图片是内置离线样图，仅用于验证图片文件保留和读取，不作为真实景点照片证据。没有输入真实服务 Key、调用付费服务或操作个人手机。证据：`upgrade/verified.json`、升级前后 XML、截图与包状态。专用模拟器验收后已关闭。

## 公开后回读

[v0.9.0](https://github.com/chengweiv5/train-trip/releases/tag/v0.9.0) 已于 2026-09-23 01:06:45（北京时间）公开并设为 Latest，非草稿、非预发布。标签及发布时 main 均为 `44f836833bad2d78d110f3e53f87d6766bd99c58`。

中文正文、两项附件及 GitHub 摘要已回读；用 `gh release download` 重新下载公开 APK 和 SHA256 后，与本地产物逐字节一致，签名、包名、版本和不可调试状态再次核验通过。证据：`published-verified.json`、`latest.json`、`remote-refs.txt`、`published-download/`。

punk-12 发布完成通知 `om_x100b641f2e8804a0c1284f4d2542f01` 已发送并回读，内容一致。后续验收记录提交仅更新文档，公开标签保持指向原发布提交。
