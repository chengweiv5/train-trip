# v0.4.0 移除复制车次入口验收

2026-09-19，用户确认移除“复制车次”。实现与可编辑设计稿已同步，底部仅保留全宽已选摘要和“打开 12306 App”。版本保持 0.4.0 / code 8。

## 改动

- 删除复制按钮、图标、剪贴板写入、行程文本生成和回调传递。
- 未选择提示改为“选好后，可打开 12306 App”；打开失败时保留选择并提示稍后重试。
- 设计稿移除复制组件及成功反馈，保留 7 个根节点、2 个可复用组件与 5 张反馈卡片。恢复快照、参考图、PDF 和设计说明均已更新。

## 验证

- `:app:assembleDebug :app:assembleDebugAndroidTest :app:lintDebug --offline` 通过；lint 0 错误、12 条既有警告。
- 专用 API 36 模拟器的 `DetailFlowTest` 返回 `OK (6 tests)`：复制入口不存在；打开 App 不查询、不改写剪贴板；失败保留选择；局部刷新失败与重试；取消及重复刷新；失效选择清除；筛选、排序与恢复；320dp / 1.3 倍字体和最后一张卡片可达。
- 大字体下主按钮位于摘要下方，点击高度至少 48dp，可见文字边界检查通过。
- 9 张 [运行截图](v0.4.0-remove-copy-runtime) 完成 OCR，共 217 段文字；4 张设计图共 141 段。未识别到复制文案，并结合原始文本和画面检查；OCR 不作为逐字无误的证明。
- Pencil 回读 324 个可见节点、148 个文字节点。0.5px 容差内无越界、同级重叠、过小交互目标、空文本、占位节点或无效组件引用。日期标签 `C3NO9` 的原始裁切标记仅为浮点舍入。
- 保存后的 `.pen` 另行复制并通过 Pencil 打开，完整层级与恢复快照一致。源文件 SHA256：`0070305d3d3fe6d38e5403e7bc419251ab117957ca7fbe60ab73cc05bb9393ec`。

构建、UI 原始日志和保存回读副本位于工作区 `.verification-private/v0.4.0/remove-copy/`。设计结构和回读证据见 [design/validation](../../design/validation)。本轮未重复真实票源查询；App 启动入口保持上一轮已验证的原生实现。

## 真机交付

Mate 60 Pro / ALN-AL00 已保留数据覆盖安装并启动。安装前、安装后和启动后的筛选设置逐字节一致；前台回读为 `cn.traintrip.app/.MainActivity`。本地 APK 与从真机拉回的 APK SHA256 一致：

`ff589d2fc283cd7a5e7587da2f4cedf7735c1f7860109b43bb542849e53fe195`

安装包：`artifacts/train-trip-v0.4.0-debug.apk`（忽略的本地构建产物）。证据与旧 APK、设置备份：`.verification-private/v0.4.0/remove-copy/phone/`。

## 回滚

- 修改前源码：`.verification-private/v0.4.0/remove-copy/source-before.tar`，基线 `2531e3c`；设计：同目录 `design-before.pen`。
- 源码与设计可通过本次提交的反向提交恢复，保留后续变更。
- 真机可用上述 `phone/before.apk` 执行 `adb -s FMR0224725012307 install -r`，不卸载、不清除应用数据。
