# v0.5.0 / v0.6.0 正式 Release 补发

日期：2026-09-21。发布前 GitHub 仅有 v0.4.0；代码推送并未创建 Release。本次补齐两版中文更新说明、生产签名 APK 和 SHA256 附件，v0.6.0 标记为 Latest。

## 版本与文件

| Release | 源码提交 | APK SHA256 |
| --- | --- | --- |
| [v0.5.0](https://github.com/chengweiv5/train-trip/releases/tag/v0.5.0) | `773e639968bd5e6affe6b1fde3ccd1da54950e09` | `65c4e54651aaf257b61bded8146f91dd99c79824b0b00ab6f2a17d17bf89e883` |
| [v0.6.0](https://github.com/chengweiv5/train-trip/releases/tag/v0.6.0) | `804e61ae9cb0d11598ff363aaec8515ee5e4a8b1` | `f8f7affef16ac7addde26aee175f7997511f4752feae2858000132e88191461b` |

v0.5.0 使用包含收藏持久化和更新说明链接修复的最终提交，版本 0.5.0 / 9，8,886,697 字节。通过 `git archive` 提取到临时隔离目录重新构建，未回退当前工作区。

v0.6.0 使用刚完成安装交付的生产候选，版本 0.6.0 / 10，8,923,689 字节；源码与已构建的 `6e0fb92` 相同，`804e61a` 仅补充交付记录。内容包含 B4 界面、齿轮图标、时段多选、车程滚轮和想去分组。

## 实际验证

- v0.5.0 `:core:test :app:assembleRelease :app:lintRelease` 成功，79 项核心测试通过，lint 0 错误 / 19 条既有警告。
- 两版均为 Release、不可调试，通过 APK v2/v3 签名、zipalign、包名和版本检查；服务 Key 模式与私有配置文件扫描均为 0 命中。
- 两版均使用 v0.4.0 的公开生产证书，SHA256 为 `a92fee2f6cce49f7bd091786a45c20768081eeef5d3b5f2e67e0544293ee1c17`。没有上传设备历史测试证书版本。
- 新建隔离模拟器 `emulator-5588`：v0.5.0 首次安装和冷启动成功，两个服务均“未配置”；添加测试收藏“保定”，覆盖升级 v0.6.0 后仍保留，appId 和首次安装时间保持不变。
- v0.6.0 冷启动成功，两个服务仍“未配置”，回拉安装 APK 哈希一致，`run-as` 被系统拒绝。未操作用户真机或调用付费服务。
- 先创建草稿并上传完整附件，核对源码提交、正文和 GitHub 附件 digest 后公开。两版均不是草稿或预发布，远端标签指向表中提交。
- 公开后重新下载各版 APK 与 `.sha256`，与本地文件、GitHub digest、校验文件逐一相符；Release 列表回读确认 v0.6.0 为 Latest。

## 兼容与留证

公开包可覆盖升级同生产证书的较低版本；用户真机历史专用证书与生产证书不同，不能直接交叉覆盖。未卸载、清数据或迁移真机；不建议为更换签名清除个人数据。

README 增加两版下载入口，v0.5.0 本地 APK 与校验文件更新为本次最终修复版。源码和设计未修改。原候选包、文档、构建日志、测试 XML、安装/升级回读、草稿及已发布元数据、下载校验保存在 `.verification-private/releases-20260921/`；临时构建目录路径记录于 `plan.json`。

如需修正发布说明可更新正文；APK 缺陷通过同证书、更高版本号发布修复版处理，不移动已发布版本标签或清除用户数据。仓库文档修改可通过反向提交回滚。
